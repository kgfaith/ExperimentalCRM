@model IEnumerable<ExperimentalCMS.Model.Admin>
@{
    ViewBag.Title = "Index";
}
@if (ViewBag.gsm != null)
{
    <div class="alert alert-success">
        @ViewBag.gsm
    </div>
}
<div class="span12">
    <h4 class="mb10">
        Admin Users</h4>
    <div class="row-fluid">
        <div class="span10">
            <p>
                @Html.ActionLink("Create New", "Create", null, new { @class = "btn btn-primary" })
            </p>
        </div>
    </div>
    <div class="row-fluid">
        <div class="span6">
            <table class="table table-striped">
                <tr>
                    <th>
                        User Name
                    </th> 
                    <th>
                        Name
                    </th>
                    <th>
                        Email
                    </th>
                    <th>
                        Last Login
                    </th>
                    <th>
                    </th>
                </tr>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.UserName)
                        </td> 
                        <td>
                            @Html.DisplayFor(modelItem => item.FirstName)  @Html.DisplayFor(modelItem => item.LastName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Email)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.LastLoginTime)
                        </td>
                        <td>
                            @Html.ActionLink("Edit", "Edit", new { id = item.AdminId }) 
                        </td>
                    </tr>
                }
            </table>
        </div>
    </div>
</div>
